# Dependencies for the cloud-GPU LLM inference service (llm_server.py).
# Install on the CUDA GPU machine only. The base model 01-ai/Yi-1.5-6B-Chat is
# pulled from HuggingFace on first launch (~12 GB). By default the QLoRA adapter
# is NOT attached (LLM_USE_ADAPTER=0 → un-fine-tuned base); set LLM_USE_ADAPTER=1
# to attach the adapter in checkpoints_llm/yi15_6b/.
#
#   pip install -r requirements-llm-server.txt
#   LLM_USE_ADAPTER=0 LLM_LOAD_4BIT=1 uvicorn llm_server:app --host 0.0.0.0 --port 6006

# Web service
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# Model loading + QLoRA inference (CUDA)
torch>=2.1.0
transformers>=4.52.0
peft>=0.19.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
sentencepiece>=0.1.99
