# Backend deps for the local host (DL predictions + remote report mode).
# On a CPU-only machine (e.g. a Mac) this is all you need: the 4 DL models run
# on CPU and report generation is delegated to the cloud-GPU service via
# LLM_REMOTE_URL. httpx is the lightweight client used for that remote call.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.6.0
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0
mne>=1.6.0          # reads real-patient .bdf EEG (load_eeg_bdf); CPU-only, deps on numpy/scipy
torch>=2.1.0
python-dotenv>=1.0.1
httpx>=0.27,<1.0
pymysql>=1.1.0     # MySQL store for the device-end (task-interface) workflow

# ── LOCAL report mode ONLY (i.e. running the LLM in-process on a CUDA GPU box).
# NOT needed on a CPU-only host using remote mode. To run the model locally,
# install these (or use requirements-llm-server.txt on the GPU machine):
#   transformers>=4.52.0
#   peft>=0.19.0
#   accelerate>=0.30.0
#   bitsandbytes>=0.43.0   # requires CUDA GPU
#   sentencepiece>=0.1.99
